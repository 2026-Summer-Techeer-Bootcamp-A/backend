# torch가 sentence-transformers의 의존성으로 딸려오는데, 기본 PyPI wheel은 CUDA
# 런타임(nvidia-cu*)까지 같이 받는다. 이 서버는 GPU가 없고 BGE-M3도 CPU로만
# 돌리므로 CPU 전용 wheel 인덱스를 우선 사용해 이미지에서 CUDA 스택을 뺀다.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.115,<0.116
uvicorn[standard]>=0.34,<0.35
prometheus-fastapi-instrumentator>=7.0,<8.0
pydantic-settings>=2.7,<3.0
sqlalchemy>=2.0,<2.1
psycopg[binary]>=3.2,<3.3
pgvector>=0.3,<0.4
bcrypt
faker
jinja2
python-multipart
pypdf>=5.0,<6.0
PyJWT
redis
email-validator
requests

# RAG vector_tool 쿼리 임베딩(BGE-M3 CPU). 저장된 공고 벡터와 동일하게
# sentence-transformers로 생성해야 같은 임베딩 공간이 된다. enable_vector_search 플래그로 게이팅.
sentence-transformers>=3.0,<4.0
orjson>=3.10,<4.0
